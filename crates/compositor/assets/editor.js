// Inline-editing client stub. Real logic (toggle wiring, autosave) lands in
// Tasks 7-9 — this only needs to exist and be valid so `serve` can hand it
// out to a page's `<script src="assets/editor.js" defer>` tag.
(function () {
  console.log("editor");
})();
